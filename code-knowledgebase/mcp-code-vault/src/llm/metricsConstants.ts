/**
 * Metrics `operation` for each vault LLM completion (tokens + latency for Stats home).
 * @see `postModelCallMetric` in `./postModelCallMetric.ts`
 */
export const METRIC_OPERATION_MODEL_CALL = 'model_call';
